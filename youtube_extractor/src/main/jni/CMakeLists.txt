
add_library( # Specifies the name of the library.
        native_extractor

        # Sets the library as a shared library.
        SHARED

        # Provides a relative path to your source file(s).
        native_extractor.cpp)